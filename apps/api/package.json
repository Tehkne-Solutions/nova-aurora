{
  "name": "@nova-aurora/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11",
    "@fastify/sensible": "^6",
    "@nova-aurora/database": "workspace:*",
    "@nova-aurora/ledger": "workspace:*",
    "fastify": "^5",
    "postgres": "^3.4",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5.9"
  }
}
